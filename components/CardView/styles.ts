import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        padding: 10,
        borderRadius: 12,
        marginHorizontal: 12,
        marginVertical: 6,
    },
    text: {
        fontSize: 40,
        fontWeight: '600',
    },
});
